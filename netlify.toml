[build]
  command = "echo 'No build step required - static site'"
  publish = "."

[build.environment]
  # Disable Stackbit/Visual Editor
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  NETLIFY_PLUGIN_VISUAL_EDITOR_DISABLED = "true"

